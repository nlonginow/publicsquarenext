name: publicsquarenext
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.0
  #webfeed: ^0.4.2
  #eof_podcast_feed: ^1.0.0  // use the local one that is updated by me
  just_audio: ^0.9.30
  audio_video_progress_bar: ^0.9.0
  flutter_html: ^0.8.2
  http: ^0.13.5
  intl: ^0.17.0
  webview_flutter: ^0.3.20+2
  flutter_pdfview: ^1.0.1
  path_provider: any
  loading_indicator: ^3.1.0
  custom_pop_up_menu: ^1.2.4
  wheel_chooser: ^1.0.0
  shared_preferences: ^2.0.15
  url_launcher: ^5.2.5
  cloud_firestore: ^4.4.4
  firebase_auth: ^4.2.10
  http_parser: ^4.0.2
  provider: ^6.0.5
  xml: ^3.7.0
  firebase_core : ^2.7.1
  eof_podcast_feed_local:
    path: ./eof_podcast_feed_local


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

#  firebase_auth: ^4.2.10
#  firebase_auth_web: ^5.2.9
#  http_parser: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - assets/images/cia.png
    - assets/images/tps60.jpg
    - assets/images/short.jpg
    - assets/images/express.jpg
    - assets/images/content.png
    - assets/images/monthly.jpg
    - assets/images/cia.jpg
    - assets/images/ridingthepine.png
    - assets/images/thinking.png
    - assets/images/blog.jpg
    - assets/images/gilbertandjack.jpg
    - assets/images/about.png
    - assets/images/register.png
    - assets/images/support.png

  uses-material-design: true
